# demihtran.github.io

AI Agent Engineer portfolio built with Next.js, TypeScript, Tailwind CSS and GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

Open the local URL printed by Next.js in your browser.

## Build

```bash
npm run build
```

The project is configured for static export and GitHub Pages deployment through `.github/workflows/deploy.yml`.
